// wM-Buster ADV — firmware version.
// Release builds pass the tag's version with -DWMB_VERSION="..." (see
// .github/workflows/build.yml); a release tag must start with this number.
#pragma once
#ifndef WMB_VERSION
#define WMB_VERSION "2.0.0"
#endif
